# PyAudio — основная библиотека для захвата и воспроизведения аудио в реальном времени.
# Обеспечивает работу с микрофоном и динамиками через PortAudio.
# На Windows рекомендуется устанавливать через pipwin, если стандартный pip не работает:
#   pip install pipwin
#   pipwin install pyaudio
# Начиная с версии 0.2.12, официальные wheels доступны на PyPI для Python 3.11+ [[1], [4]].
PyAudio>=0.2.13

# PyQt6 — современный фреймворк для создания настольных GUI-приложений.
# Используется для реализации Discord-подобного интерфейса (client_gui.py, server_gui.py).
# Включает в себя виджеты, темы, поддержку иконок и мультимедиа.
PyQt6>=6.5.0

# python-dotenv — не обязателен сейчас, но полезен для будущих настроек (например, скрытые ключи).
# Позволяет загружать переменные окружения из .env файла.
# Будет использован при расширении системы аутентификации или логирования.
python-dotenv>=1.0.0

# 💡 Для работы Push-to-Talk (PTT) в `client.py` требуется библиотека `keyboard`.  
# На Windows она работает из коробки. На Linux может потребоваться запуск от root или настройка прав для `uinput`.  
# В WSL1 функция PTT недоступна.
keyboard>=0.13.5